from queue import Queue
from collections import deque
import heapq

def bfs():
    q = Queue()
    empty_board = []
    q.put(empty_board)
    while not q.empty():
        top = q.get()
        if .. == N: return

        for i in range ...
            ....
            if True?
                q.put(...)
        